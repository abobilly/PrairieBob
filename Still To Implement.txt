  Still To Implement

  1. Layer groups completion: dynamic/meta groups, not just static
     groups.
  2. Persist layerGroups to project files (currently in runtime   
     store only).
  3. Tile action runtime wiring: action groups need to execute in 
     run/test mode (not just be editable).
  4. Persist tile action assignments on placed tiles in map data. 
  5. Expose TileTool random-brush mode in UI (tool supports it,   
     palette/options don’t expose it).
  6. .spudtile import flow in tileset UI/store (export exists,    
     import path is still incomplete).
  7. .spudtile OS-level file association in build config/runtime. 
  8. Multi-map/world UX: room/map picker instead of “load first   
     supported map”.
  9. Entity behavior editor depth: movement/state timelines and   
     interact/on-load behavior editing.                           
  10. Regression tests for panel sizing, startup modal layout, and
     TMX/Kimbar load path.      